"""Reproduction scripts for the hypeGRL paper experiments.

Paper-specific dataset loaders, baselines, per-graph descriptors, and the
figure/table runners. This package is *not* installed with the library — it
carries the heavy, niche dependencies (RDPG/ASE baselines, dataset formats)
and has no API-stability contract. It builds on the reusable primitives in
:mod:`hypegrl.evaluation`.
"""
